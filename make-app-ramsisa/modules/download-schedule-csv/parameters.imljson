[
  {
    "name": "scheduleId",
    "label": "Schedule ID",
    "type": "uuid",
    "required": true,
    "help": "UUID returned from a Generate Schedule call. The schedule must be in `completed` status."
  }
]
