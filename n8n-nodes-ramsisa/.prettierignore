dist
node_modules

# Strict mode requires eslint.config.mjs to match the @n8n/node-cli default
# byte-for-byte (single quotes), which conflicts with prettier's double-quote
# default. Excluded so the verification gate stays clean.
eslint.config.mjs

# tsconfig.json mirrors the @n8n/node-cli starter (tabs, single quotes).
# Not matching our codebase prettier defaults; keep as-is for starter parity.
tsconfig.json
